import styled from 'styled-components';

const InnerContainer = styled.div`
    display: flex;
    position: absolute;
    z-index: 1;
    height: auto;
    width: 100%;
`;

export default InnerContainer;
